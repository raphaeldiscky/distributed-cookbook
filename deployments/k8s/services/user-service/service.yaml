apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user-service
    cookbook.service: user-service
spec:
  type: ClusterIP
  selector: { app: user-service }
  ports:
    - name: http
      port: 80
      targetPort: http
