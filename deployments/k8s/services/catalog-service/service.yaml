apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  labels:
    app: catalog-service
    cookbook.service: catalog-service
spec:
  type: ClusterIP
  selector: { app: catalog-service }
  ports:
    - name: http
      port: 80
      targetPort: http
